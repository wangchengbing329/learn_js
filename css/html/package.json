{
  "name": "html",
  "version": "1.0.0",
  "description": "link img script 所有可以跨域的标签上\r  代表要和后端协商跨域 \r  1. anonymous \r   在请求头上加上Origin 属性，\r   如果 如果后端未设置 Access-control-allow-origin 请求数据会出错",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node server.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "koa": "^2.7.0",
    "koa-router": "^7.4.0",
    "koa-static": "^5.0.0"
  }
}
